# LEA-197 — Application Engine e Panel Runtime comum

Status: implementação concluída; aguardando CI e teste visual no Linux Mint.

## Objetivo

Executar aplicações Linux reais em painéis do TriView Workspace, incorporando a janela quando compatível e usando fallback externo controlado quando necessário.

## Implementação

- `PanelRuntime` reutilizável para processos e janelas;
- execução sem shell e resolução explícita do executável;
- backend X11 com localização por PID, classe e nome;
- incorporação com `xdotool windowreparent`;
- fallback para janela externa;
- `ApplicationEngine` com ciclo de vida independente;
- `ApplicationPanelAdapter` registrado no Workspace Engine;
- validação de comandos no editor de painéis;
- estados Disponível, Abrindo, Ativo, Externo, Indisponível e Erro;
- redimensionamento e encerramento controlados;
- instalador de candidato isolado;
- testes unitários e ADR-0005.

## Critérios técnicos

- [x] fundação comum para processos e janelas;
- [x] comando executado sem `shell=True`;
- [x] Application Adapter integrado ao registro;
- [x] fallback externo explícito;
- [x] Browser Engine preservado;
- [x] workspaces persistentes preservados;
- [x] testes de adaptador, ciclo de vida e fallback;
- [x] versão atualizada para `0.4.0`;
- [x] candidato isolado preparado;
- [ ] CI validada no HEAD final;
- [ ] aplicação incorporada validada no Linux Mint real;
- [ ] fallback externo validado no Linux Mint real.

## Teste de aceite esperado

1. instalar o candidato LEA-197;
2. criar ou copiar um workspace;
3. editar um painel para o tipo `application`;
4. usar inicialmente um comando simples instalado, como `xterm` ou `xed`;
5. clicar em **Abrir**;
6. confirmar estado **ATIVO** quando incorporado ou **EXTERNO** quando o programa não aceitar incorporação;
7. redimensionar a janela;
8. reabrir e fechar o TriView;
9. confirmar que a versão principal continua intacta.

## Limites

Terminal, PDF, captura, gravação, plugins, editor avançado de layouts, sessões completas e Workspace Hub permanecem nas LEAs 198–205.

## Rastreabilidade

- Linear: LEA-197;
- branch: `leonpcsn/lea-197-implementar-application-engine-e-panel-runtime-comum`;
- ADR: ADR-0005;
- trem: `train/road-to-1.0`.
