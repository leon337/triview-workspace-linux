# LEA-197 — Application Engine e Panel Runtime comum

Status: remediação concluída em código; aguardando CI e novo teste visual no Linux Mint.

## Objetivo

Executar aplicações Linux reais em painéis do TriView Workspace, incorporando a janela quando compatível e usando fallback externo controlado quando necessário.

## Implementação

- `PanelRuntime` reutilizável para processos e janelas;
- execução sem shell e resolução explícita do executável;
- backend X11 com localização por PID, classe e nome;
- incorporação com `xdotool windowreparent`;
- confirmação pós-incorporação com `xwininfo`;
- fallback para janela externa quando o gerenciador de janelas desfaz ou não confirma o reparenting;
- `ApplicationEngine` com ciclo de vida independente;
- `ApplicationPanelAdapter` registrado no Workspace Engine;
- validação de comandos no editor de painéis;
- estados Disponível, Abrindo, Ativo, Externo, Indisponível e Erro;
- redimensionamento e encerramento controlados;
- instalador de candidato isolado;
- testes unitários e ADR-0005.

## Achado no Linux Mint

Durante o primeiro aceite real:

- `x-terminal-emulator` foi incorporado corretamente e exibiu **ATIVO**;
- o Xed permaneceu em janela externa, mas o TriView exibiu **ATIVO**;
- a causa foi uma confirmação otimista: o retorno bem-sucedido do `windowreparent` era tratado como prova definitiva de incorporação;
- a remediação passou a consultar o pai X11 real da janela em verificações consecutivas;
- quando a janela não permanece no host solicitado, a sessão é classificada como **EXTERNO**.

## Critérios técnicos

- [x] fundação comum para processos e janelas;
- [x] comando executado sem `shell=True`;
- [x] Application Adapter integrado ao registro;
- [x] fallback externo explícito;
- [x] confirmação pós-reparenting antes do estado **ATIVO**;
- [x] Browser Engine preservado;
- [x] workspaces persistentes preservados;
- [x] testes de adaptador, ciclo de vida e fallback;
- [x] teste de regressão para falso positivo de incorporação;
- [x] versão mantida em `0.4.0`;
- [x] candidato isolado preparado;
- [ ] CI validada no HEAD remediado;
- [ ] aplicação incorporada validada novamente no Linux Mint real;
- [ ] fallback externo com estado **EXTERNO** validado no Linux Mint real.

## Teste de aceite esperado

1. atualizar o clone para a branch de remediação da LEA-197;
2. usar `x-terminal-emulator` como aplicação;
3. confirmar terminal dentro do painel e estado **ATIVO**;
4. usar `xed` como aplicação;
5. confirmar **ATIVO** apenas se a janela permanecer incorporada;
6. caso o Xed permaneça fora, confirmar estado **EXTERNO**;
7. redimensionar a janela;
8. reabrir e fechar o TriView;
9. confirmar que a versão principal continua intacta.

## Limites

Terminal Engine, PDF, captura, gravação, plugins, editor avançado de layouts, sessões completas e Workspace Hub permanecem nas LEAs 198–205.

## Rastreabilidade

- Linear: LEA-197;
- branch original: `leonpcsn/lea-197-implementar-application-engine-e-panel-runtime-comum`;
- branch de remediação sobre o trem: `leonpcsn/train-remediate-lea-197-x11-verification`;
- ADR: ADR-0005;
- trem: `train/road-to-1.0`.
